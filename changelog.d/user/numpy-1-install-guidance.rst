the *Dependencies* section of the `getting started page <https://timezonefinder.readthedocs.io/en/latest/0_getting_started.html#dependencies>`__ now says what to do when an environment has to keep NumPy 1: which policy sets the supported NumPy generations, that 8.2.1 is where NumPy 1 left this package's floor, and that constraining ``numpy`` makes the resolver fall back to the last release accepting it - with a virtual environment named as the better answer where the clash is with a system-managed NumPy
