Speed up ``timezone_at()`` where a coordinate's H3 cell is covered by several timezones, by reading each candidate polygon's bounding box and vertex count through a buffer view rather than through numpy indexing: ~11 % per such query on both acceleration backends, ~5 % over uniformly random coordinates, and unchanged where the cell resolves to a single zone. Answers, memory mode and construction footprint are unaffected.
