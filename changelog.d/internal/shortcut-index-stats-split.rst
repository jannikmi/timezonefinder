the data report's shortcut-index statistics are computed in four named steps rather than one pass. ``scripts/reporting.py``'s ``calculate_shortcut_index_stats`` assembles what ``count_shortcut_entries``, ``shortcut_h3_coverage``, ``shortcut_efficiency_metrics`` and ``shortcut_storage_metrics`` return, so each family's denominators are chosen where they can be read and tested rather than among forty locals of one scope, and each is typed by its own TypedDict that ShortcutIndexStats now inherits. The generated ``docs/data_report.rst`` is unchanged
