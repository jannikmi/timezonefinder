the studies under ``prototypes/`` now describe their own status accurately, which is what they are kept for: ``polygon_block_encoding.py`` no longer states its byte-aligned-versus-bit-packed question as open, since polygon layout 3 shipped the bit-packed ``B = 128`` form it recommended and measured the byte-addressed read at 2.2x the whole kernel; ``shortcut_resolution_query_bench.py`` no longer reports the polar shortcut-coverage gap as live or points a fix at ``Hex.is_special``, because the exhaustive follow-up reproduced the bad answers and re-diagnosed them as antimeridian-straddling cells, which ``crosses_antimeridian`` and ``rotate_half_turn`` resolve; two retired item ids that outlived their own shipping pull requests are rewritten to the lasting fact; and ``prototypes/README.md`` drops a link to a page that no longer exists and stops claiming the directory is untested, when ``tests/test_query_stage_profile.py`` gates two invariants of the stage ladder in the default suite. ``tests/test_contributor_memory.py`` now checks that page's links and line breaks, so the dead link would have failed rather than been read
