a development environment whose optional dependency groups have gone stale now says so instead of looking like a broken package. ``uv run`` refreshes only the default groups, so a lockfile bump that moves NumPy past the installed ``numba``'s ceiling leaves a numba that ``find_spec`` finds and ``import`` rejects - which ``timezonefinder/utils_numba.py`` degrades over by design, and which surfaced only as ``test_using_numba`` asserting ``False == True``; the test now reports the underlying ``ImportError`` and names ``make install`` as the recovery. ``make force_update`` syncs ``--all-groups`` like ``make install`` rather than performing an exact bare ``uv sync``, which pruned numba, pytest and tzfpy out of the environment it was meant to repair
