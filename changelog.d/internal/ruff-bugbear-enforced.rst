ruff enforces the ``flake8-bugbear`` family and ``RUF013`` beside its default rules, so those findings are caught by ``make hook`` and by CI rather than by ad-hoc ``ruff check --select`` runs. ``B023`` and ``B905`` are ignored for now, with the reason recorded at the ignore. Two ``warnings.warn`` calls gained ``stacklevel=2`` so they point at the caller, and what ``prototypes/`` is exempt from is stated once, in ``[tool.ruff.lint]``, so a bare ``uv run ruff check`` and the pre-commit hook cannot disagree.
