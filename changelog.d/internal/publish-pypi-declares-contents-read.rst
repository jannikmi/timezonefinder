the PyPI publishing job declares the ``contents: read`` its checkout needs. A ``permissions:`` block is exhaustive - naming ``id-token: write`` for Trusted Publishing zeroed every other scope - so the job would have failed at its first step had it ever reached one. A new test asserts that any job declaring permissions declares every scope its own steps require, including those behind the local composite actions it calls
