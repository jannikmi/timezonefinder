``prototypes/query_stage_profile.py``'s stage ladder now measures the query path rather than the checked public API in front of it: its ``zone_name_from_id`` rung binds ``zone_names.name_of``, the accessor ``timezone_at`` calls, where it bound the public wrapper and so priced a sign check the lookup never pays (58-64 ns against 37-38, 8.2-8.7 % of a unique query's ladder rather than 5.2-5.3 %); and its bounding-box and hole rungs now stop at the candidate the lookup stops at rather than running to the end of the candidate slice, which had them opening 2.2 % more candidates per ambiguous query and charging the difference to the point-in-polygon rung. Both ladder tables in the module's ``FINDINGS`` block are re-taken accordingly - no lookup code moved, so the block, point-in-polygon and A/B tables there still describe this tree - and ``tests/test_query_stage_profile.py`` now asserts both properties structurally, since a drifted rung produces a plausible table rather than a failure.
