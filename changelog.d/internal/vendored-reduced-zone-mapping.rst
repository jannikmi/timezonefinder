the test suite converts its expected zone names through upstream's own merged-zone lookup, so an expectation written against the full dataset also holds against the reduced ``timezones-now`` data that ``update_data.sh --dataset=same-since-now`` compiles. ``timezone-names-with-oceans-Now.json`` is vendored verbatim at ``tests/fixtures/reduced_zones/mapping.json``, with the release, size and SHA-256 it was verified against recorded beside it; ``scripts/upstream_release.py vendor-zone-mapping`` re-takes it with every data update and refuses an asset upstream replaced under a tag already used, and ``tests/test_reduced_zone_mapping.py`` re-hashes the committed bytes offline - which is what makes "a reduced-zone mapping comes from upstream or not at all" a check rather than an intention. The conversion is gated on the packaged data actually being the reduced variant, detected from the packaged zone names, so an ordinary run over the full dataset converts nothing. Solves issue #334
