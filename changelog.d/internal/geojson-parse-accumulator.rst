the GeoJSON converter's parse threads one accumulator object through its three call levels instead of ten separate parameters. ``scripts/timezone_data.py``'s ``ParseAccumulator`` owns the eight collections and two counters the parse builds up, so a hole is attributed to a polygon and a ring to its vertex count by an object's own state rather than by argument position - where several neighbouring parameters shared a type and a transposed pair at any of the three call sites type-checked. The converter's output over the packaged boundaries is byte-identical
