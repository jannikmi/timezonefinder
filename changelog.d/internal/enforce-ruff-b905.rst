Internal: ``B905`` (a ``zip()`` without an explicit ``strict=``) is now enforced, which empties the ruff ``ignore`` list. All 27 sites carry ``strict=True``: each pairs iterables a mismatch in whose lengths would be a bug rather than a shape. The one exception is ``tests/test_data_update_guard.py``, where ``zip(releases, releases[1:])`` walks consecutive pairs and is deliberately one short.
