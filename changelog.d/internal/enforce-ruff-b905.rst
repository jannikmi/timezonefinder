Internal: ``B905`` (a ``zip()`` without an explicit ``strict=``) is now enforced, which empties the ruff ``ignore`` list. Of the 27 sites, 23 carry ``strict=True``, pairing iterables a mismatch in whose lengths would be a bug rather than a shape. Four are deliberately not strict: three unpack parsed GeoJSON, where ``strict=`` constrains each position's arity and would reject the elevation a position is allowed to carry, and one walks consecutive releases as ``zip(releases, releases[1:])``.
