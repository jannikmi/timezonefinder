The candidate-ordering guarantee is now asserted against the real lookup: permuting a multi-zone cell's candidates within one zone cannot move a ``TimezoneFinder`` answer, a point contained by a single zone's candidates survives any permutation at all, a cross-zone rotation does move answers (so those two assert something), ``TimezoneFinderL`` answers the final candidate's zone, and the converter's geometric safety gate refuses pole and antimeridian cells before reading any geometry.
