``tests/test_release_workflows.py`` now classifies every command under ``scripts/`` by the irreversible step it must precede and fails on one that is unclassified, so a check that nothing invokes is no longer a state the repository reaches by omission - the ordering assertion for a release gate follows from that classification instead of having to be written per gate, and it names what a silent pass would cost
