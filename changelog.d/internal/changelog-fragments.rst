a change files one changelog fragment under ``changelog.d/`` instead of editing the unreleased section of ``CHANGELOG.rst``. The directory - ``user/`` or ``internal/`` - carries the placement, so no two concurrent pull requests touch the same lines of the last file every non-exempt change had to edit. ``scripts/changelog_fragments.py`` validates them (classification, one bullet on one line, unique slugs, deterministic order), previews the assembled section, and folds them into the changelog at release time; assembly is additive, so existing bullets are never rewritten and the release's end-state rewrite stays the deliberate act the changelog policy describes. ``CHANGELOG.rst`` remains the published artifact, so nothing about installing or reading the package changes
