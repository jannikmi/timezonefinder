The automated data-update workflows now check that the ``DATA_UPDATER_APP_ID`` and ``DATA_UPDATER_PRIVATE_KEY`` secrets are both set before minting a token, and the fallback issue the weekly check opens names an unconfigured GitHub App as the cause instead of asking for the data to be regenerated by hand.
