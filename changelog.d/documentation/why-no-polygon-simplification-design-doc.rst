New design page `Why timezonefinder doesn't simplify polygons <https://timezonefinder.readthedocs.io/en/latest/why_no_simplification.html>`__: why simplification concentrates error at the borders a lookup can get wrong, how it would break shared borders and hole deduplication, which lossless mechanisms deliver its benefits instead, the options considered, and the conditions that would reopen the decision.
