namespace timezonefinder.flatbuf;

table Polygon {
  coords:[int]; // Combined x and y coordinates stored as a single vector
}

table PolygonCollection {
  polygons:[Polygon];
}

root_type PolygonCollection;
