namespace timezonefinder.flatbuf;

table Polygon {
  coords:[uint]; // Combined x and y coordinates stored as a single vector
}

table PolygonCollection {
  polygons:[Polygon];
}

root_type PolygonCollection;
